name: Native 2.0

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Native 2.0 smoke test
        run: |
          echo "Native 2.0 workflow is running"
