[pytest]
addopts = -q
pythonpath = src tas_pythonetics/src tas-recursion-conversion .
markers =
    topology_safety: Isolates topological boundary and drift-resilience safety checks.
    phoenix_recovery: Isolates deterministic Phoenix recovery protocol validation checks.
