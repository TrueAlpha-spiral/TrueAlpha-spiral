name: Verify TAS DNA Manifest
on: [push, pull_request]
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify timeline manifest integrity
        run: |
          sha256sum -c TAS_DNA_timeline.yaml.sha256
